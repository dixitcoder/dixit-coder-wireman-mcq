import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DixitCoder Wireman MCQ',
  webDir: 'dist'
};

export default config;
